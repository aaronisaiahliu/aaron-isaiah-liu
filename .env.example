# Set the final public website origin (no trailing slash).
NEXT_PUBLIC_SITE_URL=
# Server-only contact delivery configuration. Never prefix these with NEXT_PUBLIC_.
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=
RESEND_API_KEY=
# A cryptographically random value, at least 32 bytes, shared by all server instances.
CONTACT_FORM_SECRET=
